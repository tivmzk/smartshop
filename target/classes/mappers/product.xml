<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="product">
	<sql id="search">
		<where>
			<if test="search == 1">
				code = #{keyword}
			</if>
			<if test="search == 2">
				name LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="search == 3">
				spec LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="search == 4">
				category LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="search == 5">
				manufacture LIKE CONCAT('%', #{keyword}, '%')
			</if>
		</where>
	</sql>
	<select id="list" resultType="Product">
		SELECT * 
		FROM PRODUCT 
		<include refid="search"/>
		LIMIT #{offset}, #{perPage}
	</select>
	<insert id="add">
		INSERT INTO product
		VALUES (NULL, #{name}, #{spec}, #{category}, #{manufacture} ,#{price})
	</insert>
	<select id="item" resultType="Product">
		SELECT * 
		FROM product
		WHERE code = #{code}
	</select>
	<update id="update">
		UPDATE product
		SET
		name = #{name},
		spec = #{spec},
		category = #{category},
		manufacture = #{manufacture},
		price = #{price}
		WHERE code = #{code}
	</update>
	<delete id="delete">
		DELETE FROM product
		WHERE code = #{code}
	</delete>
	<select id="total" resultType="Integer">
		SELECT COUNT(*)
		FROM product
		<include refid="search"/>
	</select>
</mapper>